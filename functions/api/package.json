{
    "name": "api",
    "version": "0.0.0",
    "main": "dist/index.js",
    "dependencies": {
        "@repo/types": "workspace:*",
        "axios": "^1.11.0"
    },
    "devDependencies": {
        "@repo/tsconfig": "workspace:*",
        "vitest": "^2.0.5"
    },
    "scripts": {
        "build": "tsc",
        "test": "vitest",
        "dev": "tsc --watch"
    }
}